# Apollo 客户端应用标识
# 网关作为灰度入口，必须固定 app.id，避免 ApolloNoAppIdPlaceHolder
app.id=octopus-gateway

# Apollo 环境标识
# 本地默认 LOCAL，生产环境可通过 JVM 参数 -Denv 覆盖
env=LOCAL

# Apollo config_server_url地址
# 本地默认指向本机 Apollo Portal/ConfigService
# 生产环境可通过 JVM 参数 -Dapollo.meta 覆盖
apollo.meta=http://127.0.0.1:8080
